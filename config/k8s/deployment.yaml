apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-app
  labels:
    app: weather-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: weather-app
  template:
    metadata:
      labels:
        app: weather-app
    spec:
      containers:
        - name: weather-app
          image: weather-app:latest
          ports:
            - containerPort: 5000
          env:
            - name: OPENWEATHER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: weather-secrets
                  key: api-key
            - name: PORT
              value: "5000"
            - name: REDIS_URL
              value: "redis://redis-service:6379"
            - name: LOG_LEVEL
              value: "INFO"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: weather-secrets
                  key: database-url
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: weather-secrets
                  key: db-user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: weather-secrets
                  key: db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: weather-secrets
                  key: jwt-secret
            - name: RATE_LIMIT_PER_HOUR
              value: "100"
            - name: SMTP_HOST
              valueFrom:
                secretKeyRef:
                  name: weather-secrets
                  key: smtp-host
            - name: SMTP_PORT
              value: "587"
            - name: SMTP_USER
              valueFrom:
                secretKeyRef:
                  name: weather-secrets
                  key: smtp-user
            - name: SMTP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: weather-secrets
                  key: smtp-password
            - name: SLACK_WEBHOOK_URL
              valueFrom:
                secretKeyRef:
                  name: weather-secrets
                  key: slack-webhook-url
            - name: SENTRY_DSN
              valueFrom:
                secretKeyRef:
                  name: weather-secrets
                  key: sentry-dsn
            - name: ALERT_THRESHOLD_CELSIUS
              value: "35"
            - name: WEATHER_UNITS
              value: "metric"
            - name: FORECAST_DAYS
              value: "7"
